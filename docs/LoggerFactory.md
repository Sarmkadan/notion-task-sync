# LoggerFactory

A factory class responsible for creating and managing file-based loggers in the `notion-task-sync` project. It provides mechanisms to generate loggers for specific types, manage log file paths, and handle log rotation and cleanup. The factory ensures logs are written to a configurable file location while supporting basic log lifecycle operations.

## API

### `LoggerFactory`
